---
import BaseLayout from '../layouts/BaseLayout.astro';
import ImagePlaceholder from '../components/ImagePlaceholder.astro';
import SiteHeader from '../components/SiteHeader.astro';

const categories = ['Entrenamientos', 'Torneos', 'Campamentos', 'Comunidad', 'Eventos', 'Cancha'];
---

<BaseLayout
  title="Boom Playa | Galería"
  description="Galería de fotos y videos de entrenamientos, torneos y campamentos de Boom Playa."
>
  <SiteHeader />

  <main>
    <section class="page-hero">
      <div>
        <p class="eyebrow">Galería</p>
        <h1>Fotos y videos por categoria.</h1>
        <p>
          Una navegación simple para organizar entrenamientos, torneos,
          campamentos, comunidad y eventos.
        </p>
      </div>
      <ImagePlaceholder label="Placeholder para destacado de galería" tone="coral" />
    </section>

    <section class="section">
      <p class="eyebrow">Categorias</p>
      <h2>Contenido visual listo para cargar.</h2>
      <div class="gallery-grid large">
        {categories.map((category) => (
          <article>
            <ImagePlaceholder label={`Placeholder: ${category}`} tone="small" />
            <h3>{category}</h3>
          </article>
        ))}
      </div>
    </section>
  </main>
</BaseLayout>
